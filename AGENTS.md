# Crossline project location

The canonical project directory is:

`/Users/nikhilvadhawana/Desktop/CXSOURCE/REPO/crossline`

Use this directory for all future Crossline work, commands, edits and file links.
Do not create or maintain another project copy in Documents.
`/Users/nikhilvadhawana/Documents/cxstrike` is only a compatibility symlink for the existing open task; it is not the canonical location.

Development: `npm run dev` (port 3000).
Verification: `npm test`, `npm run build`, and `node tests/browser.js` (requires installed Google Chrome).
